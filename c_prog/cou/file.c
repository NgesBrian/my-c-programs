#include<stdio.h>
int main()
{
	FILE *fp;
	FILE *fo;
	char string[]="name.txt";
	fp=fopen(string,"r+");
	fo=fopen(type.txt,"r+");
	if(fp==NULL)
		printf("Colud not open file\n");
	fscanf(fp,"%s",name);
	fprintf(op, "%s",name);
return 0;
}
